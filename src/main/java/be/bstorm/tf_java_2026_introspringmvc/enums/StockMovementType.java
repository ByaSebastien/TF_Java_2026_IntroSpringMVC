package be.bstorm.tf_java_2026_introspringmvc.enums;

/**
 * Énumération des types de mouvements de stock possibles.
 */
public enum StockMovementType {
    OUTGOING,   // Sortie (commande client expédiée)
    INCOMING    // Entrée (réception fournisseur)
}
